import { Metadata } from "next"

export const metadata: Metadata = {
  title: "Settings | Vendora",
  description: "Customize the feel of your dashboard"
}

export default function Settings() {
  return (
    <div className="flex flex-col">
      <h2>Settings</h2>
    </div>
  )
}